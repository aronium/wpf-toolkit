<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="ProgressBar">
        <Setter Property="Height" Value="16" />
        <Setter Property="Template">

            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border BorderBrush="{DynamicResource BorderBrush}" 
                            Background="{DynamicResource Background}" SnapsToDevicePixels="True" BorderThickness="1" Padding="1">
                        <Border x:Name="PART_Track" Padding="0,1" Margin="1,0,1,0">
                            <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left" SnapsToDevicePixels="True" Fill="{DynamicResource Accent}" />
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>

        </Setter>

    </Style>
</ResourceDictionary>